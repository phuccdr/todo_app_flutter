import 'package:flutter/widgets.dart';

class FocusTabView extends StatelessWidget {
  const FocusTabView({super.key});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
